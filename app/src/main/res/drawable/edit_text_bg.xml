<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Couleur de fond -->
    <solid android:color="@android:color/white" />

    <!-- Coins arrondis -->
    <corners android:radius="12dp" />

    <!-- Bordure -->
    <stroke
        android:color="#CCCCCC" android:width="1dp" />

    <!-- Ombre légère -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
